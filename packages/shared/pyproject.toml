[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "gameassets-shared"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["pydantic>=2,<3"]

[tool.setuptools.packages.find]
where = ["."]
include = ["shared*"]